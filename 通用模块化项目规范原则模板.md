# 通用模块化项目规范原则模板

版本：1.0

## 核心设计原则

-   模块独立
-   数据隔离
-   明确输入输出
-   降低隐藏依赖
-   提高维护性
-   支持长期扩展

核心思想：

> 模块提供能力，业务拥有状态。\
> 数据明确传递，结果明确返回。\
> 不依赖隐藏关系。

------------------------------------------------------------------------

## 数据所有权原则

数据属于创建者。

每个文件、模块只管理自己的：

-   变量
-   数组
-   状态
-   对象
-   资源

禁止：

-   读取其他模块内部变量
-   修改其他模块状态
-   依赖隐藏逻辑

------------------------------------------------------------------------

## 函数设计原则

函数内部数据只能来自：

1.  参数传入
2.  自身临时变量

正确：

    result = Function_Update(data)

流程：

    当前数据
     ↓
    传入函数
     ↓
    函数处理
     ↓
    返回结果
     ↓
    当前模块更新

禁止：

-   全局变量依赖
-   隐藏变量引用
-   偷改外部状态

------------------------------------------------------------------------

## 函数数据流原则

函数可以：

-   接收参数
-   读取明确资源
-   处理数据
-   返回结果

函数不能：

-   搜索未知数据
-   引用其他模块状态
-   修改调用者内部变量

函数应该完成自己的职责闭环：

    输入
     ↓
    处理
     ↓
    输出

------------------------------------------------------------------------

## 模块通信原则

模块之间禁止：

-   共享变量
-   共享内部状态
-   直接修改对方数据

推荐：

-   参数
-   返回值
-   文件
-   消息
-   句柄
-   API
-   明确协议

------------------------------------------------------------------------

## 公共库设计原则

公共库只提供能力。

公共库不保存业务状态。

公共库不知道：

-   项目用途
-   调用者身份
-   业务流程

公共函数不强制：

-   启动
-   暂停
-   退出
-   状态流程

------------------------------------------------------------------------

## 资源管理原则

谁创建，谁释放。

资源包括：

-   文件
-   窗口
-   连接
-   定时器
-   按键状态
-   对象

优先正常退出：

    发送退出请求
     ↓
    目标程序收到
     ↓
    自己释放资源
     ↓
    正常退出

------------------------------------------------------------------------

## 命名原则

名称必须表达：

-   所属范围
-   实际含义
-   数据用途

避免：

    data
    temp
    value
    state
    obj

推荐：

    user_config
    window_handle
    task_running
    input_keys

------------------------------------------------------------------------

## 扩展原则

新增功能优先：

    新增模块
     ↓
    组合使用

避免：

    不断修改核心代码

------------------------------------------------------------------------

## 最终原则

模块层：

    模块提供能力
    业务拥有状态

数据层：

    谁创建
    谁管理

函数层：

    参数进入
    处理逻辑
    返回结果

通信层：

    明确接口
    禁止隐藏依赖

最终：

> 不共享内部状态。\
> 不依赖隐藏关系。\
> 不通过引用修改别人数据。\
> 所有变化必须经过明确的数据流。\
> 每个模块、每个函数都有清晰责任边界。
